flask>=3.0

# --- Windows extras (all optional; the remote falls back to media keys) ------
# Media session bindings, for track title / artist / art / seek. These are
# compiled C++ extensions, so they only install where a prebuilt wheel exists:
#   winsdk                       -> wheels for Python 3.8 - 3.12
#   winrt-Windows.Media.Control  -> wheels for Python 3.9 - 3.13
#
# The [all] extra is required, not cosmetic: the bare package depends only on
# winrt-runtime and omits the Windows.Foundation / Windows.Media packages its
# own types need, so importing it fails.
winsdk>=1.0.0b10 ; sys_platform == "win32" and python_version < "3.13"
winrt-Windows.Media.Control[all]>=3.2 ; sys_platform == "win32" and python_version >= "3.13"

# System volume slider. Pure Python, but pulls in psutil, which is compiled.
pycaw>=20240210 ; sys_platform == "win32"
comtypes>=1.2 ; sys_platform == "win32"
